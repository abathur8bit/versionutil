name: versionutil
description: Command line application that produces a version file using Semantic Versioning rules, and keeps track of a build number.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  xml: ^6.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
